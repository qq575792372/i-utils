[**@ivujs/i-utils**](README.md)

***

[@ivujs/i-utils](README.md) / 函数

# 函数

## 函数

### debounce()

> **debounce**\<`T`\>(`fn`, `delay`, `immediate`): (`this`, ...`args`) => `void`

防抖函数

#### 类型参数

##### T

`T` *extends* (...`args`) => `any`

#### 参数

##### fn

`T`

目标函数

##### delay

`number` = `1000`

延迟时间，单位毫秒，默认 1*1000 毫秒

##### immediate

`boolean` = `true`

是否立即执行，默认true

#### 返回

返回function()

> (`this`, ...`args`): `void`

##### 参数

###### this

`ThisParameterType`\<`T`\>

###### args

...`Parameters`\<`T`\>

##### 返回

`void`

#### Description

事件执行后，在延迟时间内如果再次执行，会清空定时器重新延迟执行，举例：用户在输入框进行输入搜索，最终是会获取到最后一次输入，节约请求资源

***

### throttle()

> **throttle**\<`T`\>(`fn`, `interval`): (`this`, ...`args`) => `void`

节流函数

#### 类型参数

##### T

`T` *extends* (...`args`) => `any`

#### 参数

##### fn

`T`

目标函数

##### interval

`number` = `1000`

时间间隔，单位毫秒，默认1000毫秒

#### 返回

返回function()

> (`this`, ...`args`): `void`

##### 参数

###### this

`ThisParameterType`\<`T`\>

###### args

...`Parameters`\<`T`\>

##### 返回

`void`

#### Description

高频触发时，在指定时间间隔内只执行一次，举例：监听页面滚动，不会频繁触发，只会在固定时间内获取一次

***

### sleep()

> **sleep**(`delay`): `Promise`\<`unknown`\>

睡眠延迟执行

#### 参数

##### delay

`number` = `1000`

延迟时间，单位毫秒，默认1000毫秒

#### 返回

`Promise`\<`unknown`\>

#### Description

需要配合 async/await 来达到延迟效果
