# 脱敏

## 函数

### formatStartOf()

```ts
function formatStartOf(
   value: string, 
   start: number, 
   len: number): string;
```

指定位置的字符串转为星号

#### 参数

##### value

`string`

字符串参数

##### start

`number`

转换开始位置

##### len

`number`

显示星号的长度

#### 返回

`string`

返回转化后字符串

***

### formatStartOfName()

```ts
function formatStartOfName(value: string): string;
```

姓名中间转为星号

#### 参数

##### value

`string`

姓名

#### 返回

`string`

返回转化后字符串

***

### formatStartOfMobile()

```ts
function formatStartOfMobile(
   value: string, 
   start: number, 
   len: number): string;
```

手机号码固定位数转为星号

#### 参数

##### value

`string`

手机号码

##### start

`number` = `3`

前缀长度，默认3位

##### len

`number` = `4`

显示星号的长度，默认4位

#### 返回

`string`

返回转化后字符串

***

### formatStartOfIDCard()

```ts
function formatStartOfIDCard(
   value: string, 
   start: number, 
   len: number): string;
```

身份证号码固定位数转为星号

#### 参数

##### value

`string`

身份证号码

##### start

`number` = `4`

前缀长度，默认4位

##### len

`number` = `8`

显示星号的长度，默认8位

#### 返回

`string`

返回转化后字符串

***

### formatStartOfBankCard()

```ts
function formatStartOfBankCard(
   value: string, 
   start: number, 
   len: number): string;
```

银行卡号固定位数转为星号

#### 参数

##### value

`string`

银行卡号

##### start

`number` = `4`

前缀长度，默认4位

##### len

`number` = `11`

显示星号的长度，默认10位

#### 返回

`string`

返回转化后字符串
