/* 分页 */
export function getLimitStart({ page, pageSize }) {}

export function getLimitEnd({ page, pageSize }) {}

export function getTotalPage({ page, pageSize }) {}

/**
 * 获得彩虹页码数
 * @param page
 * @param pageSize
 * @param pagerCount
 */
export function getRainbowPager({ page, pageSize, pagerCount }) {}

export function getPrevPage({ page, totalPage }) {}

export function getNextPage({ page, totalPage }) {}
