import * as SM2 from "./SM2.js";

// 获取密钥
/**
 * sm2 加密
 * @param {String} str 字符串
 * @param {Object} options 配置
 * @returns {String} 返回加密后的字符串
 */
export function sm2Encrypt(str, options) {
  // return SM2(str, options);
}

// 加密解密
