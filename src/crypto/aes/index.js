import AES from "./aes.js";

console.log(3333, AES);

export function encrypt(str, key) {
  let textAsBytes = AES.utils.utf8.toBytes(str);
  console.log(222, textAsBytes);
  let keyAsBytes = AES.utils.utf8.toBytes(key);
  let aes = new AES.AES([
    0x2b, 0x7e, 0x15, 0x16, 0x28, 0xae, 0xd2, 0xa6, 0xab, 0xf7, 0x15, 0x88, 0x09, 0xcf, 0x4f, 0x3c,
  ]);
  console.log(9999, aes);
  return aes.encrypt([0x2b, 0x7e, 0x15, 0x16, 0x28, 0xae, 0xd2, 0xa6, 0xab, 0xf7, 0x15, 0x88, 0x09, 0xcf, 0x4f, 0x3c]);
}

export function decrypt(str, key) {
  return AES.decrypt(str, key);
}
